{
  "name": "speakbuddy",
  "scripts": {
    "build": "cd speakbuddy/frontend && npm install && npm run build && cp -r dist ../backend/public",
    "start": "cd speakbuddy/backend && npx tsx server.ts"
  },
  "dependencies": {
    "tsx": "^4"
  }
}
